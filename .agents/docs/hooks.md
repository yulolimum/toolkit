# Hooks

React and React Native hooks for lifecycle and interaction behavior, including app state, authorization, debouncing, repeated presses, query refresh, and navigation prevention. Hooks expose values and props; presentation belongs in components.
