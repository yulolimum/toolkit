# Hooks

React and React Native hooks for lifecycle and interaction behavior, including app state, authorization, debouncing, repeated presses, query refresh, and navigation prevention. Hooks generally expose values and props; components own presentation. The pull-to-refresh hook returns the native control because it is only used as a list prop.
