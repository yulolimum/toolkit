# Utils

Standalone helper modules for arrays, objects, strings, dates, colors, images, timers, errors, geography, and provider composition. Utilities are stateless and independently copyable; configured or long-lived behavior belongs in other domains.
